---
phase: 04-polish-performance-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/Nav.astro]
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "The Nav row (brand + 5 links) wraps onto multiple lines instead of overflowing/clipping when the viewport is narrower than ~520-550px"
    - "The Nav still renders as a single row on desktop/tablet widths (768px+), matching current visual appearance"
  artifacts:
    - path: "src/components/Nav.astro"
      provides: "Responsive flex-wrap nav container so the 5-link row degrades gracefully below the smallest Tailwind breakpoint"
      contains: "flex-wrap"
  key_links:
    - from: "src/components/Nav.astro outer div"
      to: "src/components/Nav.astro ul"
      via: "both carry flex-wrap so the nav can drop to a second line at any width, not just at a fixed breakpoint"
      pattern: "flex-wrap"
---

<objective>
Fix the concrete, already-diagnosed Nav.astro mobile overflow bug identified in 04-RESEARCH.md: the nav's `<div class="flex items-center justify-between">` and its `<ul class="flex gap-2">` have zero responsive classes, and the combined width of the "Gerson Ramos" brand text plus 5 nav links (`px-4 py-2` padding each) exceeds available width well before Tailwind's 640px `sm:` breakpoint — causing horizontal overflow/clipping on real mobile devices (375px, 390px).

Purpose: POLISH-01 requires the site to be usable on mobile and desktop. This is the one concrete responsive bug found in the phase-4 audit (every other page/section already uses `sm:` breakpoints adequately per 04-RESEARCH.md).
Output: `src/components/Nav.astro` updated with a zero-JS `flex-wrap` fix so the nav degrades to a wrapped multi-line layout instead of overflowing on narrow viewports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-performance-launch/04-RESEARCH.md
</context>

<interfaces>
Current Nav.astro contents (read the actual file in full before editing — this is a paraphrased description, not a literal fenced reproduction, to avoid confusing frontmatter parsers with an embedded frontmatter-delimiter block):

Frontmatter script section defines a `NavLink` interface (label, href), builds a `homeHref` from `import.meta.env.BASE_URL`, a `navLinks` array of 5 entries (Home/About/Experience/Projects/Contact, with anchor and `/projects/` hrefs), and a `normalizedCurrentPath` computed from `Astro.url.pathname` for `aria-current` matching.

Template section:
- `<nav class="bg-surface border-b border-border px-4 py-6 sm:px-8">`
  - `<div class="mx-auto flex max-w-5xl items-center justify-between">` — contains the brand `<a>` (text "Gerson Ramos") and the `<ul class="flex gap-2">` link list
  - Each link is an `<li><a href={link.href} aria-current={...} class="inline-block rounded px-4 py-2 text-sm font-semibold leading-snug text-foreground hover:text-accent focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent aria-[current=page]:text-accent aria-[current=page]:underline">{link.label}</a></li>`
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add flex-wrap responsive classes to Nav.astro</name>
  <files>src/components/Nav.astro</files>
  <read_first>
    - src/components/Nav.astro (full file, shown above in interfaces block — do not skip this read, the exact current class strings must be matched for the edit)
  </read_first>
  <action>
Edit `src/components/Nav.astro` to make exactly these two class changes (per 04-RESEARCH.md Architecture Pattern 1 — do not introduce any JavaScript/hamburger toggle, this must stay a pure CSS fix):

1. On the outer `<div>` (currently `class="mx-auto flex max-w-5xl items-center justify-between"`), change to:
   `class="mx-auto flex max-w-5xl flex-wrap items-center justify-between gap-y-2"`

2. On the `<ul>` (currently `class="flex gap-2"`), change to:
   `class="flex flex-wrap justify-center gap-1 sm:gap-2"`

Do not change anything else in the file — the `<a>` brand link, the `navLinks` array, the `<li>`/`<a>` mapping, `aria-current` logic, and the outer `<nav>` element's classes (`bg-surface border-b border-border px-4 py-6 sm:px-8`) all stay exactly as they are. This is a 2-class-string edit only.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "flex-wrap" src/components/Nav.astro` returns exactly `2` (one on the outer div, one on the ul)
    - `grep "gap-y-2" src/components/Nav.astro` matches the outer div line
    - `grep "gap-1 sm:gap-2" src/components/Nav.astro` matches the ul line
    - `npm run build` completes with exit code 0 and no new warnings/errors
    - No `<script>` tag or client-side JS was added to Nav.astro (`grep -c "<script" src/components/Nav.astro` returns `0`)
  </acceptance_criteria>
  <done>Nav.astro's outer container and ul both carry flex-wrap so the 5-link nav row wraps onto a second line instead of overflowing/clipping below ~520-550px viewport width, verified structurally via grep and via a clean `npm run build`. Full visual confirmation at 375px/390px/768px/1024px happens in the phase's final comprehensive human-verify checkpoint (04-05-PLAN.md), not here — this task is the automatable code-level fix.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds with the two flex-wrap classes present in Nav.astro. This is a structural/build-level check only; the phase's final wave (04-05-PLAN.md) includes the actual manual resize verification at mobile breakpoints as part of the comprehensive pre-launch checkpoint, avoiding checkpoint fatigue from verifying this one small fix in isolation.
</verification>

<success_criteria>
- `src/components/Nav.astro` outer div and ul both contain `flex-wrap`
- No JavaScript was introduced (zero-JS philosophy preserved)
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-performance-launch/04-01-SUMMARY.md`
</output>
