---
phase: 04-polish-performance-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [scripts/optimize-images.mjs, public/projects/sales-real-time/thumbnail.png, public/projects/postgres-clean-process-superstore/diagram.png, src/components/ProjectCard.astro, src/pages/projects/[slug].astro]
autonomous: true
requirements: [POLISH-02]

must_haves:
  truths:
    - "public/projects/sales-real-time/thumbnail.png is recompressed to roughly 37KB (down from 102,330 bytes), same 950x541 pixel dimensions, no visible quality loss"
    - "public/projects/postgres-clean-process-superstore/diagram.png is recompressed to roughly 34KB (down from 124,204 bytes), same 1550x1232 pixel dimensions, no visible quality loss"
    - "Both raster img tags include explicit width/height attributes matching real pixel dimensions, preventing layout shift, plus loading=lazy and decoding=async"
  artifacts:
    - path: "scripts/optimize-images.mjs"
      provides: "One-off, re-runnable Sharp recompression script for the 2 oversized project PNGs (author-time only, not part of the Astro build)"
      contains: "sharp"
    - path: "public/projects/sales-real-time/thumbnail.png"
      provides: "Recompressed thumbnail (~37KB, same 950x541 dimensions)"
    - path: "public/projects/postgres-clean-process-superstore/diagram.png"
      provides: "Recompressed diagram (~34KB, same 1550x1232 dimensions)"
  key_links:
    - from: "src/components/ProjectCard.astro img tag"
      to: "public/projects/sales-real-time/thumbnail.png"
      via: "explicit width=950 height=541 loading=lazy decoding=async attributes added to the existing img tag"
      pattern: 'width="950"'
    - from: "src/pages/projects/[slug].astro img tag"
      to: "public/projects/postgres-clean-process-superstore/diagram.png"
      via: "conditional width/height applied only for the postgres-clean-process-superstore project (its diagramPath ends in .png), not the 3 SVG-diagram projects"
      pattern: "endsWith"
---

<objective>
Recompress the site's 2 oversized raster images in place (no resize, no format change — per 04-RESEARCH.md's measured findings both PNGs are already at appropriate 2x-retina display dimensions and shrink 63-73% via same-dimensions Sharp recompression alone), and add explicit width/height/loading/decoding attributes to the img tags that render them to prevent layout shift and defer offscreen loading.

Purpose: POLISH-02 requires images to be compressed and correctly sized for fast page loads. 04-RESEARCH.md explicitly recommends against migrating to Astro's image() Content Collection helper for just 2 images — keep the existing plain public/ + BASE_URL string convention, just recompress in place.
Output: Both PNGs reduced in file size with unchanged visual dimensions; img tags hardened against layout shift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-performance-launch/04-RESEARCH.md
</context>

<interfaces>
Current ProjectCard.astro img block (src/components/ProjectCard.astro, lines 17-23):

  {project.data.thumbnail && (
    <img
      src={`${baseUrl}/${project.data.thumbnail.replace(/^\//, '')}`}
      alt={`${project.data.title} thumbnail`}
      class="rounded border border-border"
    />
  )}

Current [slug].astro img block (src/pages/projects/[slug].astro, lines 33-39):

  {project.data.diagramPath && (
    <img
      src={`${baseUrl}/${project.data.diagramPath.replace(/^\//, '')}`}
      alt={`${project.data.title} architecture diagram`}
      class="rounded border border-border"
    />
  )}

Note: the [slug].astro diagram img is shared by ALL 5 projects — 3 use SVG diagrams (analise-cenarios, redshift-dbt, sql-pyspark-translator) and 1 uses the PNG diagram (postgres-clean-process-superstore). Hardcoding width=1550 height=1232 unconditionally would be visually wrong for the 3 SVG diagrams. Task 2 below applies the fixed dimensions only when diagramPath ends in .png.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create and run the Sharp recompression script</name>
  <files>scripts/optimize-images.mjs, public/projects/sales-real-time/thumbnail.png, public/projects/postgres-clean-process-superstore/diagram.png</files>
  <read_first>
    - .planning/phases/04-polish-performance-launch/04-RESEARCH.md (Code Examples section — exact Sharp API call and measured before/after byte counts)
  </read_first>
  <action>
Create scripts/optimize-images.mjs with exactly this content (verified working against this repo's Sharp installation per 04-RESEARCH.md):

import sharp from 'sharp';
import { statSync } from 'node:fs';

const files = [
  'public/projects/sales-real-time/thumbnail.png',
  'public/projects/postgres-clean-process-superstore/diagram.png',
];

for (const file of files) {
  const before = statSync(file).size;
  const buf = await sharp(file).png({ quality: 85, compressionLevel: 9 }).toBuffer();
  await sharp(buf).toFile(file);
  const after = statSync(file).size;
  const pct = Math.round((1 - after / before) * 100);
  console.log(file, before, '->', after, 'bytes, -' + pct + '%');
}

Run it from the repo root:
cd "C:\Users\gerso\dev\projeto_portifolio_local"
node scripts/optimize-images.mjs

Expected console output (per 04-RESEARCH.md measured results): sales-real-time/thumbnail.png shrinks from 102330 bytes to roughly 37000-38000 bytes (~64% reduction), and postgres-clean-process-superstore/diagram.png shrinks from 124204 bytes to roughly 34000-35000 bytes (~73% reduction). Do not resize or convert to WebP — 04-RESEARCH.md confirmed same-dimensions PNG recompression alone captures nearly all available savings and both source images are already at appropriate 2x-retina width for their display containers.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.statSync('public/projects/sales-real-time/thumbnail.png').size;const d=fs.statSync('public/projects/postgres-clean-process-superstore/diagram.png').size;console.log(t,d);process.exit(t>60000||d>60000?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/optimize-images.mjs exists and imports sharp
    - public/projects/sales-real-time/thumbnail.png file size is under 60000 bytes (measured ~37KB)
    - public/projects/postgres-clean-process-superstore/diagram.png file size is under 60000 bytes (measured ~34KB)
    - PNG pixel dimensions unchanged: thumbnail.png still 950x541, diagram.png still 1550x1232 (verify via `node -e "const b=require('fs').readFileSync('PATH');console.log(b.readUInt32BE(16),b.readUInt32BE(20))"` for each file)
  </acceptance_criteria>
  <done>Both oversized PNGs are recompressed in place to roughly 37KB and 34KB respectively with unchanged pixel dimensions, confirmed by file size and PNG header dimension checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add width/height/loading/decoding attributes to raster img tags</name>
  <files>src/components/ProjectCard.astro, src/pages/projects/[slug].astro</files>
  <read_first>
    - src/components/ProjectCard.astro (full file, shown in interfaces block)
    - src/pages/projects/[slug].astro (full file, shown in interfaces block)
  </read_first>
  <action>
In src/components/ProjectCard.astro, update the thumbnail img tag to add explicit dimensions and lazy-loading hints (this image is always the sales-real-time PNG at 950x541 — thumbnail is currently only set on that one project per src/content/projects/*.mdx):

  {project.data.thumbnail && (
    <img
      src={`${baseUrl}/${project.data.thumbnail.replace(/^\//, '')}`}
      alt={`${project.data.title} thumbnail`}
      width="950"
      height="541"
      loading="lazy"
      decoding="async"
      class="rounded border border-border"
    />
  )}

In src/pages/projects/[slug].astro, the diagram img is shared across projects with either PNG or SVG diagrams. Add a computed check for the PNG case (only postgres-clean-process-superstore) and apply width=1550/height=1232 conditionally, leaving SVG diagrams without fixed pixel dimensions (SVGs scale correctly without them). Add this line in the frontmatter script section, near the existing `const baseUrl = import.meta.env.BASE_URL;` line:

  const isPngDiagram = project.data.diagramPath?.endsWith('.png');

Then update the diagram img block to:

  {project.data.diagramPath && (
    <img
      src={`${baseUrl}/${project.data.diagramPath.replace(/^\//, '')}`}
      alt={`${project.data.title} architecture diagram`}
      width={isPngDiagram ? '1550' : undefined}
      height={isPngDiagram ? '1232' : undefined}
      loading="lazy"
      decoding="async"
      class="rounded border border-border"
    />
  )}

loading="lazy" and decoding="async" apply to all 5 projects' diagrams (SVG and PNG alike) since all diagrams render below the immediate viewport fold in the article layout — this is safe for SVGs too.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - grep 'width="950"' src/components/ProjectCard.astro matches
    - grep 'loading="lazy"' src/components/ProjectCard.astro matches
    - grep 'isPngDiagram' src/pages/projects/[slug].astro matches (both the const declaration and its usage)
    - grep 'loading="lazy"' src/pages/projects/[slug].astro matches
    - npm run build completes with exit code 0 (all 5 project pages build without error, including the 3 SVG-diagram projects which must not receive incorrect 1550x1232 dimensions)
  </acceptance_criteria>
  <done>ProjectCard.astro's thumbnail img and [slug].astro's diagram img both declare explicit width/height (where a real raster source exists) plus loading=lazy and decoding=async, preventing layout shift and deferring offscreen image loads, without breaking the 3 SVG-diagram case studies.</done>
</task>

</tasks>

<verification>
`npm run build` succeeds after both recompression and img-attribute changes. File sizes for both PNGs are confirmed under 60KB (down from 102KB/124KB) with unchanged pixel dimensions, and all 5 project case study pages (3 SVG-diagram, 1 PNG-diagram, 1 PNG-thumbnail) still build and render correctly.
</verification>

<success_criteria>
- Both target PNGs recompressed to roughly 37KB and 34KB, same pixel dimensions
- ProjectCard.astro and [slug].astro img tags carry width/height/loading/decoding attributes
- `npm run build` passes with no regressions across all 5 case studies
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-performance-launch/04-02-SUMMARY.md`
</output>
