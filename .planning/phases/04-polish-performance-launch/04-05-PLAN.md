---
phase: 04-polish-performance-launch
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified: []
autonomous: false
requirements: [POLISH-01, POLISH-02, POLISH-03, POLISH-04]

must_haves:
  truths:
    - "The Nav does not overflow or clip at 375px/390px viewport widths, and renders as a single row at 768px/1024px, on the live deployed site"
    - "View-source on the live homepage and a live case study page shows real, non-placeholder meta description and Open Graph tags"
    - "The user has walked the entire live v1 site (home, about, experience, contact, projects gallery, all 5 case studies, resume downloads, footer links) end to end and approved it as launch-ready"
  artifacts: []
  key_links:
    - from: "human-verify checkpoint approval"
      to: "v1 launch status"
      via: "user's explicit resume-signal response (e.g. \"approved\")"
      pattern: "n/a — verified via user response, not a grep-able file"
---

<objective>
Run the final automated spot-checks confirming POLISH-01 through POLISH-04 are live and correct, then present a single comprehensive human-verify checkpoint covering the ENTIRE v1 site — not just this phase's changes — since this is the last plan of the last phase before v1 is considered launched. The checkpoint scope spans all 4 phases: Phase 1's foundation/nav/footer, Phase 2's narrative content (about/experience/contact/resume), Phase 3's projects gallery and all 5 case studies, and this phase's responsive/image/SEO fixes.

Purpose: This is the final pre-launch gate. 04-RESEARCH.md and the phase goal both frame this as "fully verified before public launch" — a holistic human judgment call, not a mechanical check, modeled on the final checkpoints already used in Phase 1 (01-04) and Phase 2 (02-04).
Output: User approval that v1 is launch-ready, or a specific, actionable list of issues to fix before considering v1 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-polish-performance-launch/04-RESEARCH.md
@.planning/phases/01-foundation-deployment-pipeline/01-04-SUMMARY.md
@.planning/phases/02-core-narrative-pages/02-04-SUMMARY.md
@.planning/phases/03-case-study-template-project-content/03-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final automated SEO/artifact spot-check against the live site</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/04-polish-performance-launch/04-RESEARCH.md (Pattern 4 — expected head tag shape; Pitfall 3/4 — absolute og:image URLs, robots.txt discoverability)
  </read_first>
  <action>
Run these checks against the live URL (https://gersonlramos.github.io/gersonlopesr.github.io/) and report the actual output for each before presenting the human-verify checkpoint in Task 2:

  curl -s https://gersonlramos.github.io/gersonlopesr.github.io/ | grep -o '<meta name="description"[^>]*>'
  curl -s https://gersonlramos.github.io/gersonlopesr.github.io/ | grep -o '<meta property="og:[a-z]*"[^>]*>'
  curl -s https://gersonlramos.github.io/gersonlopesr.github.io/projects/sales-real-time/ | grep -o '<meta property="og:image"[^>]*>'
  curl -s https://gersonlramos.github.io/gersonlopesr.github.io/sitemap-index.xml | grep -o "<loc>[^<]*</loc>" | head -5
  curl -s -o /dev/null -w "%{http_code}" https://gersonlramos.github.io/gersonlopesr.github.io/robots.txt

Expected: the homepage meta description is non-empty and reads the real portfolio description (not a placeholder); at least og:type/og:url/og:title/og:description are present on the homepage; the sales-real-time case study's og:image tag contains an absolute https:// URL (not a relative /projects/... path); the sitemap lists real page URLs under the gersonlopesr.github.io base path; robots.txt returns HTTP 200.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://gersonlramos.github.io/gersonlopesr.github.io/</automated>
  </verify>
  <acceptance_criteria>
    - Homepage curl output contains a non-empty meta name="description" tag with real content (not the string "TODO" or empty)
    - Homepage curl output contains at least 4 distinct og: meta tags (type, url, title, description)
    - sales-real-time case study curl output contains an og:image tag whose content starts with "https://" (absolute URL, not relative)
    - sitemap-index.xml curl output contains at least one <loc> entry
    - robots.txt returns HTTP 200
  </acceptance_criteria>
  <done>All 4 POLISH requirements' machine-verifiable artifacts (meta description, Open Graph tags, sitemap, robots.txt) are confirmed live and correctly formed via direct curl checks against the production URL, providing the automated evidence base for the human-verify checkpoint in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Comprehensive final v1 launch verification (all 4 phases)</name>
  <action>
What was built: the complete v1 portfolio site, now including Phase 4's polish: a flex-wrap responsive Nav (no more mobile overflow), recompressed project images (~37KB/~34KB, down from ~100-124KB), full meta description + Open Graph + Twitter card tags on every page, a build-time XML sitemap, robots.txt, and a linkinator-verified zero-broken-links live site (Task 1 of 04-04-PLAN.md already confirmed this programmatically). This checkpoint is the final human sign-off before v1 is considered launched.

How to verify:
Visit https://gersonlramos.github.io/gersonlopesr.github.io/ and walk through the entire v1 site:

**1. Responsive check (POLISH-01) — this phase's fix:**
   - Using browser devtools device toolbar (or by resizing the window), check the Nav at 375px, 390px, 768px, and 1024px widths.
   - Confirm: no horizontal scrollbar, no clipped or overlapping nav links at any width; the Nav wraps onto a second line at narrow widths instead of overflowing.

**2. Homepage / narrative (Phase 2, re-verified):**
   - Confirm the hero states the hybrid Data Scientist + Data Engineer positioning.
   - Confirm the Experience section lists real CV entries (role, organization, period, context, impact).
   - Confirm both "Download Resume (PDF)" and "Baixar Currículo (PT)" buttons download real files.
   - Confirm the footer shows working Email/LinkedIn/GitHub/Kaggle icon links.

**3. Projects gallery + case studies (Phase 3, re-verified):**
   - Visit /projects/ — confirm all 5 projects appear with visible technology tags and the sales-real-time thumbnail image loads (now recompressed — should still look visually sharp, not blurry or degraded).
   - Open each of the 5 case studies — confirm each follows problem→approach→impact structure, states business impact (not just a technical metric), links out to GitHub (not embedded code), and that the 3 Data Engineering case studies show an architecture diagram (postgres-clean-process-superstore's diagram.png should look visually sharp despite being recompressed).
   - Confirm the flagship demo link (analise-cenarios) opens a working externally-hosted interactive demo.

**4. SEO tags (POLISH-04) — this phase's fix:**
   - Right-click -> "View Page Source" on the homepage and on one case study page.
   - Confirm a real `<meta name="description">` tag is present (not empty, not a placeholder string).
   - Confirm `og:title`/`og:description`/`og:url` tags are present.

**5. Links (POLISH-03) — already automated-verified in 04-04-PLAN.md Task 2:**
   - Spot-check by clicking the LinkedIn, GitHub, and Kaggle footer links and confirming they open the correct real profiles in a new context.

Report any issue found at any step with enough detail to file it as a fix (URL, viewport width, exact text/element affected).
  </action>
  <resume-signal>Type "approved" if the entire v1 site (all 4 phases) is launch-ready, or describe each specific issue found (e.g. "Nav still overflows at 375px" or "case study X missing impact statement").</resume-signal>
</task>

</tasks>

<verification>
v1 is verifiably launch-ready when: (1) Task 1's automated curl checks all pass against the live production URL, (2) the user has walked the full site across all 4 phases at multiple viewport widths, and (3) the user's resume-signal response is an explicit approval with no unresolved issues.
</verification>

<success_criteria>
- All 4 POLISH requirements (POLISH-01 through POLISH-04) confirmed live via automated checks
- User has explicitly approved the complete v1 site as launch-ready
- No open issues remain from the comprehensive walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-performance-launch/04-05-SUMMARY.md`
</output>
