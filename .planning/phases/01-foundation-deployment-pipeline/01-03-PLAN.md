---
phase: 01-foundation-deployment-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements: [SETUP-01]

must_haves:
  truths:
    - "Pushing to main triggers a GitHub Actions workflow that builds the Astro site and deploys it to GitHub Pages"
    - "The workflow has the exact permissions required for Pages deployment (pages: write, id-token: write)"
    - "The workflow uses the official withastro/action and actions/deploy-pages actions, not a hand-rolled gh-pages branch push"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Two-job (build, deploy) CI/CD pipeline to GitHub Pages"
      contains: "actions/deploy-pages@v5"
  key_links:
    - from: ".github/workflows/deploy.yml build job"
      to: ".github/workflows/deploy.yml deploy job"
      via: "needs: build"
      pattern: "needs: build"
---

<objective>
Create the GitHub Actions workflow that automatically builds and deploys the Astro site to GitHub Pages on every push to `main`, using the official first-party actions (`withastro/action` + `actions/deploy-pages`) rather than a hand-rolled `gh-pages` branch push.

Purpose: SETUP-01 requires the site to "automatically redeploy via GitHub Actions whenever changes are pushed" — this workflow file is that mechanism. It only depends on 01-01's `package.json`/lockfile existing (for the build step), not on the layout components from Plan 02, so it runs in parallel with Plan 02.
Output: `.github/workflows/deploy.yml`, verified for correct structure (permissions, job dependency, action versions) via local grep-based checks — actual live-run verification happens in Plan 04 after the repo is pushed to GitHub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-deployment-pipeline/01-RESEARCH.md
@.planning/phases/01-foundation-deployment-pipeline/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the GitHub Actions deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - G:\Meu Drive\Projetos\projeto_portifolio\package.json (confirm `build` script exists and matches `astro build`, from 01-01)
  </read_first>
  <action>
Create the directory `.github/workflows/` and the file `deploy.yml` inside it with exactly this content (verified against the live `withastro/action`, `actions/checkout`, and `actions/deploy-pages` GitHub release APIs on 2026-07-03, per 01-RESEARCH.md):

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v7
      - name: Install, build, and upload your site
        uses: withastro/action@v6

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
```

Do not substitute a custom "build then push to gh-pages branch" workflow — `withastro/action@v6` auto-detects the package manager from `package-lock.json` (npm, per 01-01) and handles the Pages artifact upload internally; `actions/deploy-pages@v5` handles the actual deploy. This is the official, maintained pattern (01-RESEARCH.md "Don't Hand-Roll").
  </action>
  <verify>
    <automated>cd "G:\Meu Drive\Projetos\projeto_portifolio" && grep -c "pages: write" .github/workflows/deploy.yml && grep -c "id-token: write" .github/workflows/deploy.yml && grep -c "withastro/action@v6" .github/workflows/deploy.yml && grep -c "actions/deploy-pages@v5" .github/workflows/deploy.yml && grep -c "needs: build" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy.yml` exists
    - Contains `branches: [main]` under the `push` trigger
    - Contains `pages: write` and `id-token: write` under `permissions`
    - Contains `uses: withastro/action@v6` in the `build` job
    - Contains `uses: actions/deploy-pages@v5` in the `deploy` job
    - Contains `needs: build` in the `deploy` job (job dependency wired correctly)
  </acceptance_criteria>
  <done>deploy.yml exists with correct triggers, permissions, and the official build+deploy job pair — ready to run on first push (verified live in Plan 04).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm build-artifact exclusion in .gitignore</name>
  <files>.gitignore</files>
  <read_first>
    - G:\Meu Drive\Projetos\projeto_portifolio\.gitignore (created by 01-01's scaffold copy — verify contents, do not blindly overwrite)
  </read_first>
  <action>
Read the existing `.gitignore` (copied from the Astro scaffold in 01-01 Task 1). It should already contain `dist/`, `.astro/`, `node_modules/`, `.env`, and `.DS_Store` by default (Astro's scaffold ships these). Confirm each of these five patterns is present.

If any of the five patterns is missing, append the missing line(s) to `.gitignore` (do not remove or reorder existing lines). If all five are already present, make no changes — do not rewrite the file unnecessarily.

This matters for SETUP-01: if `dist/` or `node_modules/` were accidentally committed, the repo would bloat and the CI build step could behave inconsistently with stale committed build output shadowing fresh builds.
  </action>
  <verify>
    <automated>cd "G:\Meu Drive\Projetos\projeto_portifolio" && grep -c "dist/" .gitignore && grep -c "node_modules/" .gitignore && grep -c ".astro/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `dist/`, `node_modules/`, `.astro/`, `.env`, and `.DS_Store` (each on its own line, in any position)
    - `git check-ignore dist` (after a build has produced a `dist/` dir) exits 0
  </acceptance_criteria>
  <done>Build artifacts and OS/dependency cruft are confirmed excluded from git tracking before the first push.</done>
</task>

</tasks>

<verification>
Confirm `.github/workflows/deploy.yml` parses as valid YAML structure (correct indentation, no tab characters) by running `npm run build` (unrelated to the workflow file directly, but confirms the repo is still in a buildable state) and visually reviewing the file's structure matches the exact content specified in Task 1. Full live verification (Actions run actually succeeding) happens in Plan 04 after the first push — this plan cannot verify a live GitHub Actions run because the remote repository does not exist yet.
</verification>

<success_criteria>
- `.github/workflows/deploy.yml` exists with the exact triggers, permissions, and job structure specified
- `.gitignore` excludes `dist/`, `node_modules/`, `.astro/`, `.env`, `.DS_Store`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment-pipeline/01-03-SUMMARY.md`
</output>
