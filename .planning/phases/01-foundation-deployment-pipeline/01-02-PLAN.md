---
phase: 01-foundation-deployment-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/404.astro
autonomous: true
requirements: [SETUP-03]

must_haves:
  truths:
    - "Every page (home + 404) renders the same Nav and Footer via a single shared layout"
    - "Footer shows Email, LinkedIn, GitHub, Kaggle links in that fixed order, each with a visible sr-only accessible label"
    - "Footer icon links have a minimum 44x44px tappable area (WCAG 2.5.5)"
    - "Visiting a non-existent path renders the custom 404 page (heading 'Page not found.'), not a blank/generic error"
    - "The home page H1 is the largest, highest-contrast element on the page (Display role, 32px/600)"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Persistent shell wrapping Nav + page content + Footer"
      contains: "<slot"
    - path: "src/components/Nav.astro"
      provides: "Persistent nav bar with brand + data-driven link list"
      contains: "Gerson Lopes"
    - path: "src/components/Footer.astro"
      provides: "Persistent footer with contact/profile links"
      contains: "mailto:gersonlopesr@gmail.com"
    - path: "src/pages/index.astro"
      provides: "Placeholder home page"
      contains: "BaseLayout"
    - path: "src/pages/404.astro"
      provides: "Custom 404 error page using the shared layout"
      contains: "Page not found."
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "import + <BaseLayout> wrapper"
      pattern: "import BaseLayout"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Nav.astro and Footer.astro"
      via: "component import"
      pattern: "import (Nav|Footer)"
---

<objective>
Build the persistent navigation/footer shell (SETUP-03) as a single shared `BaseLayout.astro` component that every page imports, plus a placeholder home page and a custom 404 page that both exercise it. This is the Astro-native translation of the Jekyll `_layouts/default.html` + `_includes/nav.html`/`footer.html` pattern from 01-RESEARCH.md.

Purpose: No later phase (2-4) should ever need to touch nav/footer markup again — every new page just wraps its content in `<BaseLayout>`. Getting this right once here is what "persistent" nav/footer means.
Output: `BaseLayout.astro`, `Nav.astro`, `Footer.astro`, `index.astro`, `404.astro` — building on 01-01's Astro scaffold, Tailwind theme tokens, and installed `lucide-astro` icon library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-deployment-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-deployment-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundation-deployment-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- Theme tokens from 01-01's src/styles/global.css @theme block — these generate real Tailwind v4
     utility classes. Use them directly (bg-background, bg-surface, text-accent, border-border,
     text-muted-foreground) — do NOT use arbitrary-value [var(--color-x)] syntax. -->

Tokens available as utilities:
- `bg-background` / `text-background` -> #0a0e14 (dominant, page background)
- `bg-surface` / `text-surface` -> #111827 (nav/footer background)
- `bg-accent` / `text-accent` / `border-accent` -> #22d3ee (hover/focus/active ONLY — never a fill)
- `bg-foreground` / `text-foreground` -> #e2e8f0 (default text)
- `text-muted-foreground` -> #94a3b8 (de-emphasized text)
- `border-border` -> #1e293b (hairline borders)
- `font-sans` (Inter Variable, default) / `font-mono` (JetBrains Mono Variable, not used this phase)

`lucide-astro@^0.556.0` is already installed (01-01 Task 3). Import icons as named exports, e.g.
`import { Mail, Linkedin, Github, BarChart3 } from 'lucide-astro';` and render as
`<Mail aria-hidden="true" size={22} />`.

<note>
Every `.astro` file below is shown as TWO separate blocks: the "frontmatter script" block
(goes between Astro's standard opening and closing frontmatter fence — a line containing
exactly three hyphens, before and after) and the "template" block (goes after the closing
fence). This split is deliberate formatting for this plan document only — write the final
`.astro` file as a single file with the standard fence delimiter wrapping the script block.
</note>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build Nav.astro and Footer.astro components</name>
  <files>src/components/Nav.astro, src/components/Footer.astro</files>
  <read_first>
    - G:\Meu Drive\Projetos\projeto_portifolio\src\styles\global.css (confirm exact token names from 01-01)
    - G:\Meu Drive\Projetos\projeto_portifolio\.planning\phases\01-foundation-deployment-pipeline\01-UI-SPEC.md (Copywriting Contract, Spacing Scale exceptions, Color sections)
  </read_first>
  <action>
Create `src/components/Nav.astro`. Frontmatter script (wrap in the standard Astro frontmatter fence):

```typescript
interface NavLink {
  label: string;
  href: string;
}

// Data-driven so Phases 2-3 can append "About", "Experience", "Projects", "Contact"
// without a layout rewrite (per 01-UI-SPEC.md Copywriting Contract).
const navLinks: NavLink[] = [
  { label: 'Home', href: '/' },
];
const currentPath = Astro.url.pathname;
```

Template (after the closing fence):

```html
<nav class="bg-surface border-b border-border px-4 py-6 sm:px-8">
  <div class="mx-auto flex max-w-5xl items-center justify-between">
    <a
      href="/"
      class="text-[20px] font-semibold leading-tight text-foreground hover:text-accent focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent"
    >
      Gerson Lopes
    </a>
    <ul class="flex gap-2">
      {navLinks.map((link) => (
        <li>
          <a
            href={link.href}
            aria-current={currentPath === link.href ? 'page' : undefined}
            class="inline-block rounded px-4 py-2 text-sm font-semibold leading-snug text-foreground hover:text-accent focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent aria-[current=page]:text-accent aria-[current=page]:underline"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</nav>
```

Create `src/components/Footer.astro`. Note: `lucide-astro` has no dedicated Kaggle brand icon, so `BarChart3` (a data/chart icon) is used as the closest thematic fit — flag this as Claude's discretion, not a locked decision. The LinkedIn and Kaggle URLs are explicit placeholders per 01-UI-SPEC.md ("placeholder until Phase 2 confirms exact URL") — do not invent real profile URLs.

Frontmatter script:

```typescript
import { Mail, Linkedin, Github, BarChart3 } from 'lucide-astro';

interface ContactLink {
  label: string;
  href: string;
  icon: typeof Mail;
}

// Fixed order per 01-UI-SPEC.md Copywriting Contract: Email, LinkedIn, GitHub, Kaggle.
// LinkedIn/Kaggle hrefs are explicit placeholders — Phase 2 confirms real profile URLs.
const contactLinks: ContactLink[] = [
  { label: 'Email', href: 'mailto:gersonlopesr@gmail.com', icon: Mail },
  { label: 'LinkedIn', href: 'https://www.linkedin.com/in/PLACEHOLDER-confirm-phase-2', icon: Linkedin },
  { label: 'GitHub', href: 'https://github.com/gersonlopesr', icon: Github },
  { label: 'Kaggle', href: 'https://www.kaggle.com/PLACEHOLDER-confirm-phase-2', icon: BarChart3 },
];
```

Template:

```html
<footer class="bg-surface border-t border-border px-4 py-6 sm:px-8">
  <div class="mx-auto flex max-w-5xl flex-col items-center gap-8 sm:flex-row sm:justify-between">
    <ul class="flex gap-2">
      {contactLinks.map(({ label, href, icon: Icon }) => (
        <li>
          <a
            href={href}
            class="flex h-11 w-11 items-center justify-center rounded text-foreground hover:text-accent focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent"
          >
            <Icon aria-hidden="true" size={22} />
            <span class="sr-only">{label}</span>
          </a>
        </li>
      ))}
    </ul>
    <p class="text-sm text-muted-foreground">&copy; 2026 Gerson Lopes</p>
  </div>
</footer>
```

`h-11 w-11` = 44x44px in Tailwind's default spacing scale (11 x 4px) — satisfies the UI-SPEC's WCAG 2.5.5 minimum touch target requirement for icon-only links.
  </action>
  <verify>
    <automated>cd "G:\Meu Drive\Projetos\projeto_portifolio" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Nav.astro` contains `Gerson Lopes` and `navLinks`
    - `src/components/Footer.astro` contains all four labels: `Email`, `LinkedIn`, `GitHub`, `Kaggle`
    - `src/components/Footer.astro` contains `mailto:gersonlopesr@gmail.com` and `https://github.com/gersonlopesr`
    - `src/components/Footer.astro` contains `h-11 w-11` and `sr-only`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Nav and Footer components exist, match the copywriting contract exactly, and build without errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build BaseLayout.astro (persistent shell)</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - G:\Meu Drive\Projetos\projeto_portifolio\src\components\Nav.astro (just created in Task 1 — import path/export shape)
    - G:\Meu Drive\Projetos\projeto_portifolio\src\components\Footer.astro (just created in Task 1)
  </read_first>
  <action>
Create `src/layouts/BaseLayout.astro`. Frontmatter script (wrap in the standard Astro frontmatter fence):

```typescript
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title?: string;
}

const { title = 'Gerson Lopes — Data Scientist & Data Engineer' } = Astro.props;
```

Template (after the closing fence):

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class="bg-background text-foreground">
    <Nav />
    <main>
      <slot />
    </main>
    <Footer />
  </body>
</html>
```

The default `title` prop matches D-06 (locked): "Gerson Lopes — Data Scientist & Data Engineer" — used verbatim in the browser tab unless a page explicitly overrides it (as 404.astro will, in Task 3).
  </action>
  <verify>
    <automated>cd "G:\Meu Drive\Projetos\projeto_portifolio" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/layouts/BaseLayout.astro` contains `<slot`, `import Nav`, `import Footer`
    - `src/layouts/BaseLayout.astro` contains the literal title string `Gerson Lopes — Data Scientist & Data Engineer`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>BaseLayout renders Nav + slot content + Footer around every page that imports it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Placeholder home page and custom 404 page</name>
  <files>src/pages/index.astro, src/pages/404.astro</files>
  <read_first>
    - G:\Meu Drive\Projetos\projeto_portifolio\src\layouts\BaseLayout.astro (just created in Task 2 — Props interface and default title)
  </read_first>
  <action>
Overwrite `src/pages/index.astro` (currently the scaffold-generated placeholder from 01-01). Frontmatter script:

```typescript
import BaseLayout from '../layouts/BaseLayout.astro';
```

Template (after the closing fence):

```html
<BaseLayout>
  <div class="mx-auto flex max-w-5xl flex-col items-center gap-4 px-4 py-12 text-center sm:py-16">
    <h1 class="text-[32px] font-semibold leading-tight text-foreground">
      Gerson Lopes — Data Scientist &amp; Data Engineer
    </h1>
    <p class="text-base leading-normal text-muted-foreground">
      Site foundation is live. Full content coming soon.
    </p>
  </div>
</BaseLayout>
```

Create `src/pages/404.astro` (Astro auto-serves this file for unmatched routes in static builds). Frontmatter script:

```typescript
import BaseLayout from '../layouts/BaseLayout.astro';
```

Template (after the closing fence):

```html
<BaseLayout title="Page not found — Gerson Lopes">
  <div class="mx-auto flex max-w-5xl flex-col items-center gap-4 px-4 py-12 text-center sm:py-16">
    <h2 class="text-xl font-semibold leading-tight text-foreground">Page not found.</h2>
    <p class="text-base leading-normal text-muted-foreground">
      That page doesn't exist or may have moved. Head back to the homepage.
    </p>
    <a
      href="/"
      class="text-sm font-semibold text-accent hover:underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent"
    >
      Back to home
    </a>
  </div>
</BaseLayout>
```
  </action>
  <verify>
    <automated>cd "G:\Meu Drive\Projetos\projeto_portifolio" && npm run build && test -f dist/404.html && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` contains `import BaseLayout` and the exact H1 text `Gerson Lopes — Data Scientist &amp; Data Engineer`
    - `src/pages/404.astro` contains `Page not found.` and `Back to home`
    - `dist/index.html` and `dist/404.html` both exist after `npm run build`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Home page and custom 404 page both render through BaseLayout with the exact copy from 01-UI-SPEC.md.</done>
</task>

</tasks>

<verification>
Run `npm run build` at the repo root — must exit 0 and produce both `dist/index.html` and `dist/404.html`. Grep both output files to confirm they contain the Nav's `Gerson Lopes` brand text and the Footer's `mailto:gersonlopesr@gmail.com` link — proving the shared layout actually renders on both pages, not just one.
</verification>

<success_criteria>
- `npm run build` succeeds and emits `dist/index.html` + `dist/404.html`
- Both output pages contain the same Nav and Footer markup (persistent shell confirmed)
- Footer contains all four contact links in the fixed order with accessible sr-only labels
- 404 page uses the exact copy from 01-UI-SPEC.md's Copywriting Contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment-pipeline/01-02-SUMMARY.md`
</output>
