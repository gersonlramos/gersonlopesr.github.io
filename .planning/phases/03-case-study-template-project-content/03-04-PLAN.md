---
phase: 03-case-study-template-project-content
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/content/projects/sales-real-time.mdx
  - src/content/projects/postgres-clean-process-superstore.mdx
  - public/projects/sales-real-time/thumbnail.png
  - public/projects/postgres-clean-process-superstore/diagram.png
autonomous: true
requirements: [PROJ-03, PROJ-05]

must_haves:
  truths:
    - "Visitor opens /projects/sales-real-time/ and reads a problem->approach->impact case study"
    - "Visitor opens /projects/postgres-clean-process-superstore/ and reads a problem->approach->impact case study"
    - "Both case studies frame impact as a self-directed practice project mirroring a real production pattern, without fabricating a business outcome they didn't have"
    - "Both case studies link to their real, public GitHub repos"
  artifacts:
    - path: "src/content/projects/sales-real-time.mdx"
      provides: "DS case study content for the pet-product sales monitoring dashboard"
      contains: "## Impact"
    - path: "src/content/projects/postgres-clean-process-superstore.mdx"
      provides: "DS case study content for the SuperStore data-quality pipeline"
      contains: "## Impact"
  key_links:
    - from: "src/content/projects/sales-real-time.mdx"
      to: "https://github.com/gersonlramos/Sales_Real_Time"
      via: "repoUrl frontmatter field"
      pattern: "repoUrl:"
    - from: "src/content/projects/postgres-clean-process-superstore.mdx"
      to: "https://github.com/gersonlramos/Postgres-Clean-Process-SuperStore"
      via: "repoUrl frontmatter field"
      pattern: "repoUrl:"
---

<objective>
Author the two Data Science case studies: `Sales_Real_Time` (pet-product sales monitoring dashboard) and `Postgres-Clean-Process-SuperStore` (data-quality/cleaning pipeline with ERD). Neither requires a diagram per schema (category `data-science` is not covered by the `superRefine` diagram enforcement), but `Postgres-Clean-Process-SuperStore` already has a real ERD image asset in its repo worth reusing if fetchable.

Purpose: Complete PROJ-03/05 for the 2 DS-tagged featured projects, closing out the 5-project content set (D-01/D-02/D-11).
Output: Two MDX case study files, with best-effort real thumbnail/diagram image reuse from the source repos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-case-study-template-project-content/03-CONTEXT.md
@.planning/phases/03-case-study-template-project-content/03-RESEARCH.md
@src/content.config.ts

<interfaces>
Schema fields this MDX frontmatter must satisfy (src/content.config.ts, set by Plan 03-01):
```typescript
{
  title: string;
  summary: string;
  category: 'data-science' | 'data-engineering';
  impactSummary: string;
  stack: string[];
  role: string;
  repoUrl: string;
  notebookUrl?: string;
  thumbnail?: string;   // optional — no diagram/thumbnail enforcement for data-science category
  diagramPath?: string; // optional for data-science; NOT required by superRefine
  demoUrl?: string;
  order: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author case study for Sales_Real_Time (with optional real thumbnail)</name>
  <files>src/content/projects/sales-real-time.mdx, public/projects/sales-real-time/thumbnail.png</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\.planning\phases\03-case-study-template-project-content\03-RESEARCH.md (GitHub Repository Audit table, `Sales_Real_Time` row — confirms real screenshots exist: `PowerBI_2.png`, `PowerBI_Tabelas.png`)
  </read_first>
  <action>
**Optional thumbnail (best-effort, do not block on failure):** Use `curl -s https://api.github.com/repos/gersonlramos/Sales_Real_Time/contents/` (and recurse into any `images`/`screenshots`/`assets` subdirectory if listed) to find the exact path of `PowerBI_2.png` or a similarly named dashboard screenshot, then download it directly with `curl -sL <raw_download_url> -o "public/projects/sales-real-time/thumbnail.png"` (create the directory first: `mkdir -p public/projects/sales-real-time`). If the API call fails, rate-limits, or no matching file is found, skip this step entirely and omit `thumbnail` from the frontmatter below — do not fabricate a placeholder image.

Optionally fetch `https://raw.githubusercontent.com/gersonlramos/Sales_Real_Time/main/README.md` via WebFetch for exact KPI/table names. If unreachable, use the confirmed evidence below.

**Content** — create `src/content/projects/sales-real-time.mdx`. Its frontmatter block (YAML, opened and closed with a `---` line at the top of the file) should contain these fields — include the `thumbnail` line ONLY if the download above succeeded; otherwise omit it entirely, do not leave it pointing at a non-existent file:
```yaml
title: "Real-Time Pet-Product Sales Monitoring Dashboard"
summary: "Power BI dashboard backed by a simulated PostgreSQL sales database, tracking KPIs, payment-type distribution, and per-seller performance for pet-product sales."
category: "data-science"
impactSummary: "Mirrors the executive KPI dashboards built for Pet Premium Distribuidora in production, reconstructed end-to-end here (schema, simulated transactional data, Power BI report) to publicly demonstrate the same sales-monitoring pattern used to support real executive decision-making."
stack: ["PostgreSQL", "Power BI", "SQL"]
role: "Sole developer"
repoUrl: "https://github.com/gersonlramos/Sales_Real_Time"
thumbnail: "/projects/sales-real-time/thumbnail.png"
order: 4
```

Then, below the closing frontmatter fence, write the MDX body with `## Problem`, `## Approach`, `## Impact` sections (2-4 sentences each, specific, not generic):
- Problem: without a consolidated view, tracking pet-product sales KPIs, payment-type mix, and per-seller performance requires manual spreadsheet work.
- Approach: a simulated PostgreSQL transactional database feeds a Power BI report with KPI cards, payment-type distribution charts, and per-seller ranking tables.
- Impact: **self-directed framing** — this reconstructs, end-to-end and on non-confidential simulated data, the same executive KPI dashboard pattern already disclosed as delivered in production at Pet Premium Distribuidora, demonstrating the skill publicly without exposing real sales data. Do not fabricate specific revenue numbers.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/projects/sales-real-time.mdx` contains `category: "data-science"`, `repoUrl: "https://github.com/gersonlramos/Sales_Real_Time"`
    - File contains `## Problem`, `## Approach`, `## Impact`
    - `## Impact` section references "Pet Premium" (self-directed framing signal) and does NOT contain a fabricated revenue/sales figure
    - If a `thumbnail:` line is present in frontmatter, the corresponding file exists at `public/projects/sales-real-time/thumbnail.png`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Sales_Real_Time case study renders with correct self-directed impact framing and a working GitHub link; thumbnail included if fetch succeeded, gracefully omitted otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Author case study for Postgres-Clean-Process-SuperStore (with optional real ERD image)</name>
  <files>src/content/projects/postgres-clean-process-superstore.mdx, public/projects/postgres-clean-process-superstore/diagram.png</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\.planning\phases\03-case-study-template-project-content\03-RESEARCH.md (GitHub Repository Audit table, `Postgres-Clean-Process-SuperStore` row — confirms a real `SuperStore_ERD.png` asset exists)
  </read_first>
  <action>
**Optional ERD reuse (best-effort, do not block on failure):** Use `curl -s https://api.github.com/repos/gersonlramos/Postgres-Clean-Process-SuperStore/contents/` to find the exact path of `SuperStore_ERD.png`, then download it with `curl -sL <raw_download_url> -o "public/projects/postgres-clean-process-superstore/diagram.png"` (create the directory first: `mkdir -p public/projects/postgres-clean-process-superstore`). If unavailable, skip and omit `diagramPath` from the frontmatter — this is optional for `data-science` category (not enforced by `superRefine`).

Optionally fetch `https://raw.githubusercontent.com/gersonlramos/Postgres-Clean-Process-SuperStore/main/README.md` via WebFetch for exact cleaning-step names (dedup, null imputation, ranking queries — already confirmed in 03-RESEARCH.md). If unreachable, use that confirmed evidence.

**Content** — create `src/content/projects/postgres-clean-process-superstore.mdx`. Its frontmatter block (YAML, opened and closed with a `---` line at the top of the file) should contain these fields — include `diagramPath` ONLY if the download above succeeded; otherwise omit it:
```yaml
title: "Data Quality & Cleaning Pipeline (SuperStore)"
summary: "PostgreSQL data-cleaning and quality pipeline applying deduplication, null imputation, and ranking analysis to the SuperStore retail dataset, backed by a documented ERD."
category: "data-science"
impactSummary: "Demonstrates the same data-quality discipline (schema design, deduplication, null-handling, validation queries) behind the Pet Premium and AI Studio experience bullets, applied here to a public dataset with a fully documented ERD."
stack: ["PostgreSQL", "SQL"]
role: "Sole developer"
repoUrl: "https://github.com/gersonlramos/Postgres-Clean-Process-SuperStore"
diagramPath: "/projects/postgres-clean-process-superstore/diagram.png"
order: 5
```

Then, below the closing frontmatter fence, write the MDX body with `## Problem`, `## Approach`, `## Impact` sections (2-4 sentences each, specific, not generic):
- Problem: raw retail transaction data (SuperStore) has duplicates, nulls, and no documented schema, making it unreliable for analysis until cleaned.
- Approach: a documented ERD (referenced above, if the image was fetched) grounds a PostgreSQL pipeline of deduplication, null imputation, and ranking/validation queries.
- Impact: **self-directed framing** — state this demonstrates the same data-quality discipline (schema design, deduplication, null-handling, validation) behind the disclosed Pet Premium and AI Studio experience bullets, shown here on a public dataset with a fully documented, inspectable ERD. Do not claim it was used in production.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/projects/postgres-clean-process-superstore.mdx` contains `category: "data-science"`, `repoUrl: "https://github.com/gersonlramos/Postgres-Clean-Process-SuperStore"`
    - File contains `## Problem`, `## Approach`, `## Impact`
    - `## Impact` section references "Pet Premium" or "AI Studio" (self-directed framing signal) and does NOT claim production usage
    - If a `diagramPath:` line is present in frontmatter, the corresponding file exists at `public/projects/postgres-clean-process-superstore/diagram.png`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Postgres-Clean-Process-SuperStore case study renders with correct self-directed impact framing and a working GitHub link; ERD image included if fetch succeeded, gracefully omitted otherwise.</done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0. Run `npm run preview` and visit both `/projects/sales-real-time/` and `/projects/postgres-clean-process-superstore/`: confirm GitHub links resolve to the real repos and pages render without broken image tags.
</verification>

<success_criteria>
- Both case studies build and render with a working GitHub link
- Both Impact sections use self-directed framing tied to real disclosed experience, without fabricated metrics
- No broken `<img>` references (thumbnail/diagram fields only present when the file actually exists)
</success_criteria>

<output>
After completion, create `.planning/phases/03-case-study-template-project-content/03-04-SUMMARY.md`
</output>
