---
phase: 03-case-study-template-project-content
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified: []
autonomous: false
requirements: [PROJ-01, PROJ-02, PROJ-03, PROJ-04, PROJ-05, PROJ-06]

must_haves:
  truths:
    - "Visitor browses a gallery of all 5 projects with visible technology tags"
    - "Visitor can open a dedicated case study page for each of the 5 projects, following problem->approach->impact"
    - "Every case study explicitly states business/practical impact, not just a technical metric"
    - "All 3 data-engineering case studies include a static architecture diagram"
    - "Every case study links to the full code on GitHub instead of embedding it"
    - "The flagship project (Analise_cenarios) links to a working externally-hosted interactive demo"
  artifacts:
    - path: "src/content/projects/"
      provides: "5 complete case study MDX files (analise-cenarios, redshift-dbt, sql-pyspark-translator, sales-real-time, postgres-clean-process-superstore)"
  key_links:
    - from: "src/pages/projects/index.astro"
      to: "src/content/projects/*.mdx"
      via: "getCollection('projects') returning exactly 5 entries"
      pattern: "getCollection"
---

<objective>
Close out Phase 3 with a cross-project automated + manual verification pass: confirm all 5 case studies build together correctly, satisfy the schema's structural constraints (diagram required for DE, valid repo URLs), and pass a human visual/functional review of the live gallery and case study pages before the phase is considered done.

Purpose: This is the phase gate — Waves 1 and 2 built the infrastructure and content independently; this plan proves they compose correctly as a whole and gets explicit human sign-off on content quality (PROJ-03's "business impact, not just a metric" requirement is manual-only per 03-VALIDATION.md).
Output: Confirmed green build across all 5 projects, and human approval of the live gallery + case study experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-case-study-template-project-content/03-VALIDATION.md
@.planning/phases/03-case-study-template-project-content/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated cross-project structural verification</name>
  <files>none (verification only, no files modified)</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\src\content.config.ts
  </read_first>
  <action>
Run these checks from the repo root (`C:\Users\gerso\dev\projeto_portifolio_local`) and confirm every one passes before proceeding to Task 2:

1. `npm run build` — must exit 0 with zero schema errors.
2. Confirm exactly 5 content files exist: `ls src/content/projects/*.mdx` must list exactly `analise-cenarios.mdx`, `postgres-clean-process-superstore.mdx`, `redshift-dbt.mdx`, `sales-real-time.mdx`, `sql-pyspark-translator.mdx`.
3. Confirm the build produced 5 case study pages: `ls dist/projects/*/index.html` (excluding the gallery's own `dist/projects/index.html`) must list 5 directories.
4. Confirm every DE-category file has a `diagramPath`: `grep -L "diagramPath:" src/content/projects/analise-cenarios.mdx src/content/projects/redshift-dbt.mdx src/content/projects/sql-pyspark-translator.mdx` must return NO output (empty = all 3 have it).
5. Confirm every file has a well-formed `repoUrl` pointing at `github.com/gersonlramos`: `grep -L "repoUrl: \"https://github.com/gersonlramos/" src/content/projects/*.mdx` must return NO output.
6. Confirm the flagship demo link exists: `grep -c "demoUrl:" src/content/projects/analise-cenarios.mdx` must return `1`.
7. Confirm the Footer bug fix from Plan 03-01 is still in place: `grep -c "gersonlopesr" src/components/Footer.astro` must return `0`.

If any check fails, fix the specific file before proceeding — do not move to Task 2 with a red build or a missing artifact.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - Exactly 5 `.mdx` files exist in `src/content/projects/`
    - All 3 DE-category files have `diagramPath:`
    - All 5 files have a `repoUrl:` pointing at `github.com/gersonlramos`
    - `analise-cenarios.mdx` has a `demoUrl:`
    - `src/components/Footer.astro` contains no `gersonlopesr` reference
  </acceptance_criteria>
  <done>All 5 case studies compose correctly into a passing build with every structural (schema-enforced) requirement satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human review of the complete Projects gallery and 5 case studies</name>
  <action>
What was built: a complete projects gallery (PROJ-01) and 5 individual case study pages (PROJ-02) covering a hybrid DS/DE portfolio — Analise_cenarios (flagship, with live external demo), redshift-dbt, SQL_PySpark_Translator, Sales_Real_Time, and Postgres-Clean-Process-SuperStore. Each case study states explicit business/practical impact (PROJ-03), the 3 DE case studies include a static architecture diagram (PROJ-04), every case study links out to its real GitHub repo instead of embedding code (PROJ-05), and the flagship links to a working interactive demo (PROJ-06).

How to verify:
1. Run `npm run preview` and open the printed local URL.
2. Click "Projects" in the nav bar — confirm the gallery shows all 5 cards with visible tech-stack tags and category badges.
3. Open each of the 5 case study pages and confirm:
   - It reads as Problem -> Approach -> Impact (not generic filler)
   - The Impact section names a concrete business/practical outcome, not just a bare technical metric
   - The 3 DE-tagged pages (Analise_cenarios, redshift-dbt, SQL_PySpark_Translator) show a readable architecture diagram
   - The "View code on GitHub" link is present and points to the real repo (spot-check 1-2 by clicking through)
4. On the Analise_cenarios case study, click "Try the live demo" and confirm `https://gersonlramos.github.io/Analise_cenarios/` loads and is interactive.
5. Confirm the footer's GitHub icon points to `github.com/gersonlramos` (not `gersonlopesr`) on any page.
  </action>
  <resume-signal>Type "approved" or describe specific issues (page/section + what's wrong)</resume-signal>
</task>

</tasks>

<verification>
`npm run build` exits 0. All 5 case study routes exist in `dist/projects/`. Human has visually confirmed the gallery, all 5 case studies, the 3 diagrams, and the flagship demo link.
</verification>

<success_criteria>
- Phase 3 success criteria from ROADMAP.md are all satisfied: gallery with tags (PROJ-01), dedicated case study pages following problem->approach->impact (PROJ-02), explicit business impact per case study (PROJ-03), DE diagrams (PROJ-04), GitHub link-outs (PROJ-05), flagship interactive demo (PROJ-06)
- Human has typed "approved" on the final checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-case-study-template-project-content/03-05-SUMMARY.md`
</output>
