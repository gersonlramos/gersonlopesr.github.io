---
phase: 03-case-study-template-project-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Footer.astro
  - src/components/Nav.astro
  - src/content.config.ts
  - src/components/ProjectCard.astro
  - src/pages/projects/index.astro
  - "src/pages/projects/[slug].astro"
autonomous: true
requirements: [PROJ-01, PROJ-02]

must_haves:
  truths:
    - "Visitor can reach a Projects page from the nav bar on any page"
    - "Visitor sees a grid of project cards with visible tech-stack tags once content exists"
    - "Visitor can open a dedicated URL per project (e.g. /projects/<slug>/) that renders that project's case study template"
    - "The site's GitHub profile link points to the real account (gersonlramos), not the broken gersonlopesr"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Extended projects Content Collection schema (category, impactSummary, diagramPath, demoUrl) with superRefine enforcing a diagram for data-engineering entries"
      contains: "data-engineering"
    - path: "src/components/ProjectCard.astro"
      provides: "Reusable card component for the gallery grid"
    - path: "src/pages/projects/index.astro"
      provides: "PROJ-01 gallery page"
      contains: "getCollection"
    - path: "src/pages/projects/[slug].astro"
      provides: "PROJ-02 dynamic case study route"
      contains: "getStaticPaths"
  key_links:
    - from: "src/components/Nav.astro"
      to: "src/pages/projects/index.astro"
      via: "navLinks array entry"
      pattern: "projects/"
    - from: "src/pages/projects/index.astro"
      to: "astro:content getCollection('projects')"
      via: "getCollection call"
      pattern: "getCollection\\('projects'\\)"
    - from: "src/pages/projects/[slug].astro"
      to: "astro:content render()"
      via: "render(project) call"
      pattern: "render\\("
---

<objective>
Build the foundational infrastructure this entire phase depends on: fix the broken GitHub link bug, add site-wide navigation to a new Projects section, extend the `projects` Content Collection schema with the fields the case-study template needs (category, impactSummary, diagramPath, demoUrl), and ship the two new routes (gallery + dynamic case study page) that all 5 project content files (Wave 2) will render through.

Purpose: This is the "define contracts" wave — the schema shape and page templates built here are the interface every subsequent content-authoring plan writes against. Nothing in Wave 2 can start correctly until this plan's schema and routes exist.
Output: Extended `src/content.config.ts`, working `/projects/` gallery route, working `/projects/[slug]/` case study route, `ProjectCard.astro` component, corrected Footer GitHub link, new "Projects" Nav entry. Builds cleanly with zero content entries (Wave 2 populates content).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-case-study-template-project-content/03-CONTEXT.md
@.planning/phases/03-case-study-template-project-content/03-RESEARCH.md
@src/pages/index.astro
@src/styles/global.css

<interfaces>
Current schema state (src/content.config.ts) BEFORE this plan's changes:
```typescript
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import { z } from 'astro/zod';

const projects = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: './src/content/projects' }),
  schema: z.object({
    title: z.string(),
    summary: z.string(),
    stack: z.array(z.string()),
    role: z.string(),
    repoUrl: z.string().url(),
    notebookUrl: z.string().url().optional(),
    thumbnail: z.string().optional(),
    order: z.number().default(0),
  }),
});

export const collections = { projects };
```

BASE_URL-safe link convention already established (src/pages/index.astro):
```typescript
const baseUrl = import.meta.env.BASE_URL;
// <a href={`${baseUrl}/resume.pdf`} download>Download Resume (PDF)</a>
```

Dark tech design tokens (src/styles/global.css @theme): `--color-background: #0a0e14`, `--color-surface: #111827`, `--color-accent: #22d3ee`, `--color-foreground: #e2e8f0`, `--color-muted-foreground: #94a3b8`, `--color-border: #1e293b`. Utility classes: `bg-background`, `bg-surface`, `text-accent`, `text-foreground`, `text-muted-foreground`, `border-border`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GitHub link bug, add Projects nav entry, extend the content schema</name>
  <files>src/components/Footer.astro, src/components/Nav.astro, src/content.config.ts</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\src\components\Footer.astro
    - C:\Users\gerso\dev\projeto_portifolio_local\src\components\Nav.astro
    - C:\Users\gerso\dev\projeto_portifolio_local\src\content.config.ts
  </read_first>
  <action>
1. In `src/components/Footer.astro`, in the `contactLinks` array, change the GitHub entry's `href` from `'https://github.com/gersonlopesr'` to exactly `'https://github.com/gersonlramos'` (per D-09: `gersonlopesr` 404s, the real account is `gersonlramos`). Do not change the `label` ('GitHub') or `icon` (Github).

2. In `src/components/Nav.astro`, add a new entry to the `navLinks` array so the final order is: Home, About, Experience, Projects, Contact. Insert this object between the Experience and Contact entries:
```typescript
{ label: 'Projects', href: `${homeHref}projects/` },
```
Note: `homeHref` is `import.meta.env.BASE_URL` which already ends in `/`, so the resulting href is e.g. `/gersonlopesr.github.io/projects/` in production and `/projects/` in dev — do not add a leading slash before `projects`.

3. Overwrite `src/content.config.ts` with exactly this content (extends the schema per 03-RESEARCH.md Pattern 1 — adds `category`, `impactSummary`, `diagramPath`, `demoUrl`, plus a `superRefine` enforcing PROJ-04: every `data-engineering` entry must have a `diagramPath`):
```typescript
import { defineCollection } from 'astro:content';
import { glob } from 'astro/loaders';
import { z } from 'astro/zod';

const projects = defineCollection({
  loader: glob({ pattern: '**/*.{md,mdx}', base: './src/content/projects' }),
  schema: z
    .object({
      title: z.string(),
      summary: z.string(),
      category: z.enum(['data-science', 'data-engineering']),
      impactSummary: z.string(),
      stack: z.array(z.string()),
      role: z.string(),
      repoUrl: z.string().url(),
      notebookUrl: z.string().url().optional(),
      thumbnail: z.string().optional(),
      diagramPath: z.string().optional(),
      demoUrl: z.string().url().optional(),
      order: z.number().default(0),
    })
    .superRefine((data, ctx) => {
      if (data.category === 'data-engineering' && !data.diagramPath) {
        ctx.addIssue({
          code: z.ZodIssueCode.custom,
          message: 'PROJ-04: data-engineering case studies must include diagramPath',
          path: ['diagramPath'],
        });
      }
    }),
});

export const collections = { projects };
```
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Footer.astro` contains `https://github.com/gersonlramos` and does NOT contain `https://github.com/gersonlopesr`
    - `src/components/Nav.astro` `navLinks` array contains a `Projects` entry positioned after `Experience` and before `Contact`
    - `src/content.config.ts` contains `category: z.enum(['data-science', 'data-engineering'])`, `impactSummary: z.string()`, `diagramPath: z.string().optional()`, `demoUrl: z.string().url().optional()`, and `superRefine`
    - `npm run build` exits 0 (empty content directory is expected at this point — a "no files found" warning is fine, a schema/syntax error is not)
  </acceptance_criteria>
  <done>Footer links to the real GitHub account, Nav exposes a Projects link, and the schema exports every field the case-study template and diagram enforcement need.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProjectCard component and gallery page (PROJ-01)</name>
  <files>src/components/ProjectCard.astro, src/pages/projects/index.astro</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\src\pages\index.astro
    - C:\Users\gerso\dev\projeto_portifolio_local\src\content.config.ts
    - C:\Users\gerso\dev\projeto_portifolio_local\src\styles\global.css
  </read_first>
  <action>
Create `src/components/ProjectCard.astro`. It accepts a single prop `project` typed as `CollectionEntry<'projects'>` and renders: optional thumbnail image (only if `project.data.thumbnail` is set — do not render a broken `<img>` if absent), title, category badge (`Data Science` or `Data Engineering`, derived from `project.data.category`), 1-line `summary`, and a tag list from `project.data.stack`. The whole card is a link to `${baseUrl}projects/${project.id}/`. Use existing dark-tech tokens: `border-border`, `bg-surface`, `text-foreground`, `text-muted-foreground`, `text-accent`, hover state `hover:border-accent`.

The file's frontmatter script section (goes between the component's opening and closing three-dash fence at the very top of the file) must contain exactly:
```typescript
import type { CollectionEntry } from 'astro:content';

interface Props {
  project: CollectionEntry<'projects'>;
}

const { project } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
const categoryLabel = project.data.category === 'data-engineering' ? 'Data Engineering' : 'Data Science';
```

The template markup (goes below the closing three-dash fence) must contain exactly:
```html
<a
  href={`${baseUrl}projects/${project.id}/`}
  class="flex flex-col gap-3 rounded border border-border bg-surface p-6 hover:border-accent"
>
  {project.data.thumbnail && (
    <img
      src={`${baseUrl}${project.data.thumbnail.replace(/^\//, '')}`}
      alt={`${project.data.title} thumbnail`}
      class="rounded border border-border"
    />
  )}
  <span class="w-fit rounded border border-border px-2 py-1 font-mono text-xs text-accent">{categoryLabel}</span>
  <h2 class="text-lg font-semibold leading-snug text-foreground">{project.data.title}</h2>
  <p class="text-base leading-normal text-muted-foreground">{project.data.summary}</p>
  <ul class="flex flex-wrap gap-2">
    {project.data.stack.map((tech) => (
      <li class="rounded border border-border px-2 py-1 font-mono text-xs text-accent">{tech}</li>
    ))}
  </ul>
</a>
```

Create `src/pages/projects/index.astro`. Its frontmatter script section must contain exactly:
```typescript
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectCard from '../../components/ProjectCard.astro';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
```

Its template markup (below the closing fence) must contain exactly:
```html
<BaseLayout title="Projects — Gerson Ramos">
  <section class="mx-auto flex max-w-5xl flex-col gap-8 px-4 py-12 sm:py-16">
    <h1 class="text-center text-2xl font-semibold leading-tight text-foreground">Projects</h1>
    <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
      {projects.map((project) => <ProjectCard project={project} />)}
    </div>
  </section>
</BaseLayout>
```
No filter/sort UI (per D-05) — this is intentionally the entire gallery implementation.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ProjectCard.astro` exists and imports `CollectionEntry` from `astro:content`
    - `src/pages/projects/index.astro` contains `getCollection('projects')` and imports `ProjectCard`
    - `npm run build` exits 0 and produces `dist/projects/index.html`
  </acceptance_criteria>
  <done>The gallery page builds and will render one card per project entry once Wave 2 content exists; zero entries today is expected and does not fail the build.</done>
</task>

<task type="auto">
  <name>Task 3: Build the dynamic case study route (PROJ-02, PROJ-04, PROJ-05, PROJ-06 rendering)</name>
  <files>src/pages/projects/[slug].astro</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\src\content.config.ts
    - C:\Users\gerso\dev\projeto_portifolio_local\src\layouts\BaseLayout.astro
  </read_first>
  <action>
Create `src/pages/projects/[slug].astro` per 03-RESEARCH.md Pattern 3 (current Astro 7 Content Layer API — `render()` imported from `astro:content`, `project.id` as the slug, not `.slug`).

The file's frontmatter script section (goes between the opening and closing three-dash fence at the top of the file) must contain exactly:
```typescript
import { getCollection, render } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  const projects = await getCollection('projects');
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { Content } = await render(project);
const baseUrl = import.meta.env.BASE_URL;
```

The template markup (goes below the closing fence) must contain exactly:
```html
<BaseLayout title={`${project.data.title} — Gerson Ramos`}>
  <article class="mx-auto flex max-w-3xl flex-col gap-6 px-4 py-12 sm:py-16">
    <header class="flex flex-col gap-2">
      <span class="w-fit rounded border border-border px-2 py-1 font-mono text-xs text-accent">
        {project.data.category === 'data-engineering' ? 'Data Engineering' : 'Data Science'}
      </span>
      <h1 class="text-2xl font-semibold leading-tight text-foreground">{project.data.title}</h1>
      <p class="text-base leading-normal text-accent">{project.data.impactSummary}</p>
      <ul class="flex flex-wrap gap-2">
        {project.data.stack.map((tech) => (
          <li class="rounded border border-border px-2 py-1 font-mono text-xs text-muted-foreground">{tech}</li>
        ))}
      </ul>
    </header>

    {project.data.diagramPath && (
      <img
        src={`${baseUrl}${project.data.diagramPath.replace(/^\//, '')}`}
        alt={`${project.data.title} architecture diagram`}
        class="rounded border border-border"
      />
    )}

    <div class="prose prose-invert max-w-none text-foreground">
      <Content />
    </div>

    <footer class="flex flex-wrap gap-4 border-t border-border pt-6">
      <a href={project.data.repoUrl} class="text-accent hover:underline">View code on GitHub &rarr;</a>
      {project.data.notebookUrl && <a href={project.data.notebookUrl} class="text-accent hover:underline">View notebook &rarr;</a>}
      {project.data.demoUrl && <a href={project.data.demoUrl} class="text-accent hover:underline">Try the live demo &rarr;</a>}
    </footer>
  </article>
</BaseLayout>
```

Note: this project has no `@tailwindcss/typography` plugin installed — the `prose prose-invert` classes are harmless no-ops without it (they simply won't apply typography styles). Do NOT install the typography plugin in this task; rely on the existing `text-foreground` color + the browser's default heading/paragraph spacing for the MDX body. If this looks visually thin, it is an acceptable tradeoff for this phase (out of scope: Phase 4 POLISH handles broader visual polish).
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/projects/[slug].astro` contains `getStaticPaths`, `render(project)`, `repoUrl`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Dynamic case study route exists and will generate one static page per project entry once Wave 2 content exists.</done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0 with zero content entries (a "no files found matching" warning for the projects glob is expected and acceptable at this point, not a failure). Confirm `dist/projects/index.html` exists. Confirm `src/components/Footer.astro` no longer contains `gersonlopesr` in a GitHub URL.
</verification>

<success_criteria>
- Nav shows a working "Projects" link on every page
- Footer's GitHub icon links to `https://github.com/gersonlramos`
- `/projects/` and `/projects/[slug]/` routes build successfully with the extended schema in place
- Schema enforces `diagramPath` for `data-engineering` category via `superRefine`
</success_criteria>

<output>
After completion, create `.planning/phases/03-case-study-template-project-content/03-01-SUMMARY.md`
</output>
