---
phase: 03-case-study-template-project-content
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - public/projects/redshift-dbt/diagram.svg
  - src/content/projects/redshift-dbt.mdx
  - public/projects/sql-pyspark-translator/diagram.svg
  - src/content/projects/sql-pyspark-translator.mdx
autonomous: true
requirements: [PROJ-03, PROJ-04, PROJ-05]

must_haves:
  truths:
    - "Visitor opens /projects/redshift-dbt/ and reads a problem->approach->impact case study with a pipeline diagram"
    - "Visitor opens /projects/sql-pyspark-translator/ and reads a problem->approach->impact case study with a pipeline diagram"
    - "Both case studies frame impact as a self-directed practice project mirroring a real production pattern, without fabricating a business outcome they didn't have"
    - "Both case studies link to their real, public GitHub repos"
  artifacts:
    - path: "src/content/projects/redshift-dbt.mdx"
      provides: "DE case study content for the dbt/Redshift medallion pipeline"
      contains: "## Impact"
    - path: "src/content/projects/sql-pyspark-translator.mdx"
      provides: "DE case study content for the SQL-to-PySpark translator"
      contains: "## Impact"
    - path: "public/projects/redshift-dbt/diagram.svg"
      provides: "Static architecture diagram for PROJ-04"
      contains: "<svg"
    - path: "public/projects/sql-pyspark-translator/diagram.svg"
      provides: "Static architecture diagram for PROJ-04"
      contains: "<svg"
  key_links:
    - from: "src/content/projects/redshift-dbt.mdx"
      to: "public/projects/redshift-dbt/diagram.svg"
      via: "diagramPath frontmatter field"
      pattern: "diagramPath:"
    - from: "src/content/projects/sql-pyspark-translator.mdx"
      to: "public/projects/sql-pyspark-translator/diagram.svg"
      via: "diagramPath frontmatter field"
      pattern: "diagramPath:"
---

<objective>
Author two self-directed Data Engineering case studies: `redshift-dbt` (medallion-style dbt pipeline on Redshift) and `SQL_PySpark_Translator` (SQL-to-PySpark query translator). Both are practice/portfolio projects (not tied to a disclosed employer engagement), so their Impact sections must follow 03-RESEARCH.md's self-directed framing pattern: name the real-world production pattern they demonstrate, without inventing a fabricated business outcome.

Purpose: Complete PROJ-03/04/05 for the remaining 2 of 3 DE-tagged featured projects (Analise_cenarios, the third, is handled in Plan 03-02).
Output: Two MDX case study files and two static SVG diagrams, building cleanly against the Wave 1 schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-case-study-template-project-content/03-CONTEXT.md
@.planning/phases/03-case-study-template-project-content/03-RESEARCH.md
@src/content.config.ts

<interfaces>
Schema fields this MDX frontmatter must satisfy (src/content.config.ts, set by Plan 03-01):
```typescript
{
  title: string;
  summary: string;
  category: 'data-science' | 'data-engineering';
  impactSummary: string;
  stack: string[];
  role: string;
  repoUrl: string;
  notebookUrl?: string;
  thumbnail?: string;
  diagramPath?: string; // REQUIRED when category is 'data-engineering' (superRefine)
  demoUrl?: string;
  order: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author diagram + case study for redshift-dbt</name>
  <files>public/projects/redshift-dbt/diagram.svg, src/content/projects/redshift-dbt.mdx</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\.planning\phases\03-case-study-template-project-content\03-RESEARCH.md (GitHub Repository Audit table, `redshift-dbt` row; Case Study Content Pattern by Project Type table)
  </read_first>
  <action>
Optionally fetch `https://raw.githubusercontent.com/gersonlramos/redshift-dbt/main/README.md` via WebFetch to confirm the exact source-table names and layer structure (the README is confirmed in 03-RESEARCH.md to already contain an ASCII architecture diagram — use it as source material). If unreachable, use the confirmed evidence below.

**Diagram** — create `public/projects/redshift-dbt/diagram.svg` using the same SVG structure/palette pattern as `public/projects/analise-cenarios/diagram.svg` from Plan 03-02 (dark background `#0a0e14`, box fill `#111827`, stroke `#1e293b`, label text `#e2e8f0`, detail text `#94a3b8`, arrows `#22d3ee`, viewBox `0 0 920 260`, 4 boxes at x=20/250/480/710 width=190 height=90 y=85, connecting arrows at y=130), with these 4 stages:
1. "Raw Sources" / "8 Northwind tables"
2. "Staging Models" / "dedup, type casting"
3. "Intermediate Models" / "enrichment, joins"
4. "Marts" / "business-ready tables (Redshift)"

**Content** — create `src/content/projects/redshift-dbt.mdx`. Its frontmatter block (YAML, opened and closed with a `---` line at the top of the file) must contain exactly these fields:
```yaml
title: "Medallion-Style dbt Pipeline on Redshift"
summary: "8-source dbt transformation pipeline on AWS Redshift applying medallion-style layering to the Northwind dataset."
category: "data-engineering"
impactSummary: "Demonstrates, on a portable public dataset, the same medallion-architecture transformation pattern (raw -> cleaned -> modeled layers) used in production at Pottencial Seguros, making the technique publicly showcase-able without exposing client data."
stack: ["dbt", "Amazon Redshift", "SQL"]
role: "Sole developer"
repoUrl: "https://github.com/gersonlramos/redshift-dbt"
diagramPath: "/projects/redshift-dbt/diagram.svg"
order: 2
```

Then, below the closing frontmatter fence, write the MDX body with `## Problem`, `## Approach`, `## Impact` sections (2-4 sentences each, specific, not generic):
- Problem: raw Northwind data across 8 sources has no consistent, trustworthy business-ready layer for analytics without a structured transformation pipeline.
- Approach: dbt models organized in staging -> intermediate -> marts layers against Redshift, applying deduplication, enrichment, and joins per the diagram above.
- Impact: **use the self-directed framing pattern** — this is a practice project, not a disclosed employer engagement. State that it demonstrates the same medallion-architecture discipline used in production at Pottencial Seguros (already disclosed in the Phase 2 Experience section), applied here to a portable public dataset specifically so the technique can be shown without exposing real client data. Do NOT invent a fabricated business metric (e.g. do not claim "reduced query time by X%" unless actually measured and stated in the repo README).
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `public/projects/redshift-dbt/diagram.svg` exists, contains `<svg` and 4 stage-box labels
    - `src/content/projects/redshift-dbt.mdx` contains `category: "data-engineering"`, `diagramPath:`, `repoUrl: "https://github.com/gersonlramos/redshift-dbt"`
    - File contains `## Problem`, `## Approach`, `## Impact`
    - `## Impact` section references "Pottencial" or "medallion" or "production" (self-directed framing signal) and does NOT contain a fabricated quantified business metric
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>redshift-dbt case study renders with diagram, correct self-directed impact framing, and a working GitHub link.</done>
</task>

<task type="auto">
  <name>Task 2: Author diagram + case study for SQL_PySpark_Translator</name>
  <files>public/projects/sql-pyspark-translator/diagram.svg, src/content/projects/sql-pyspark-translator.mdx</files>
  <read_first>
    - C:\Users\gerso\dev\projeto_portifolio_local\.planning\phases\03-case-study-template-project-content\03-RESEARCH.md (GitHub Repository Audit table, `SQL_PySpark_Translator` row)
  </read_first>
  <action>
Optionally fetch `https://raw.githubusercontent.com/gersonlramos/SQL_PySpark_Translator/main/README.md` via WebFetch for exact supported SQL clause list. If unreachable, use the confirmed evidence below (handles joins, aliases, aggregates, GROUP BY).

**Diagram** — create `public/projects/sql-pyspark-translator/diagram.svg` using the same SVG structure/palette pattern as the other diagrams in this phase (same viewBox, box coordinates, and colors as described in Task 1 above), with these 4 stages:
1. "SQL Query Input" / "raw SQL text"
2. "Lexer / Parser" / "tokenize SELECT, JOIN, GROUP BY"
3. "Intermediate Representation" / "query AST"
4. "PySpark Code Generator" / "DataFrame API output"

**Content** — create `src/content/projects/sql-pyspark-translator.mdx`. Its frontmatter block (YAML, opened and closed with a `---` line at the top of the file) must contain exactly these fields:
```yaml
title: "SQL-to-PySpark Query Translator"
summary: "Custom parser/translator that converts SQL queries (joins, aliases, aggregates, GROUP BY) into equivalent PySpark DataFrame code."
category: "data-engineering"
impactSummary: "Builds direct, hands-on fluency in the SQL-to-Spark translation layer underlying the Airflow/Apache Iceberg/Spark orchestration refactor work already disclosed in the Experience section, demonstrated here as a standalone, inspectable tool."
stack: ["Python", "PySpark", "SQL"]
role: "Sole developer"
repoUrl: "https://github.com/gersonlramos/SQL_PySpark_Translator"
diagramPath: "/projects/sql-pyspark-translator/diagram.svg"
order: 3
```

Then, below the closing frontmatter fence, write the MDX body with `## Problem`, `## Approach`, `## Impact` sections (2-4 sentences each, specific, not generic):
- Problem: teams migrating workloads from SQL-based warehouses to Spark need to hand-translate queries, which is slow and error-prone for non-trivial joins/aggregates.
- Approach: a custom lexer/parser tokenizes the SQL query, builds an intermediate representation, and generates equivalent PySpark DataFrame code — reference the diagram above and the specific clause types handled (joins, aliases, aggregates, GROUP BY).
- Impact: **self-directed framing** — state that this tool demonstrates hands-on command of the exact SQL-to-Spark translation logic underlying the "refactored Airflow orchestrations... Apache Iceberg and Spark" experience bullet already disclosed on this site, built here as a standalone, publicly inspectable artifact rather than an internal one. Do not claim it replaced a production system or fabricate adoption metrics.
  </action>
  <verify>
    <automated>cd "C:\Users\gerso\dev\projeto_portifolio_local" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `public/projects/sql-pyspark-translator/diagram.svg` exists, contains `<svg` and 4 stage-box labels
    - `src/content/projects/sql-pyspark-translator.mdx` contains `category: "data-engineering"`, `diagramPath:`, `repoUrl: "https://github.com/gersonlramos/SQL_PySpark_Translator"`
    - File contains `## Problem`, `## Approach`, `## Impact`
    - `## Impact` section references "Airflow" or "Iceberg" or "Spark" (self-directed framing signal tying back to disclosed experience) and does NOT fabricate a production-adoption claim
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>SQL_PySpark_Translator case study renders with diagram, correct self-directed impact framing, and a working GitHub link.</done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0. Run `npm run preview` and visit both `/projects/redshift-dbt/` and `/projects/sql-pyspark-translator/`: confirm diagrams render and GitHub links resolve to the real repos.
</verification>

<success_criteria>
- Both case studies build and render with diagram + working GitHub link
- Both Impact sections use self-directed framing (real-world pattern demonstrated, not a fabricated business outcome)
</success_criteria>

<output>
After completion, create `.planning/phases/03-case-study-template-project-content/03-03-SUMMARY.md`
</output>
